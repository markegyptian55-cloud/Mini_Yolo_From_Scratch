Experiment 1 Baseline -- Training Log (reconstructed)
=========================================================
NOTE: The original per-step training log was not saved. This file is
reconstructed from the summary report generated at the end of the run
(original_report.md, timestamped 2026-07-21 20:40:43). It is a
5-epoch-interval summary, not a full epoch-by-epoch log.

Run config (everything known -- nothing more exists to recover)
------------------------------------------------------------------
Framework : Custom MiniYOLO (Decoupled Head, Darknet Backbone, PANet Neck)
Device    : CPU (Intel CPU)
Epochs    : 20 (target and completed -- run finished normally, no crash)
Duration  : 15h 58m total, ~48 min/epoch average (that's the only
            per-epoch timing figure that survives -- no per-epoch
            breakdown, since no raw log/results.csv was kept)
Optimizer : switched to AdamW mid-run (per report narrative)
Changes made during the run (per report narrative):
  - restructured loss weights
  - added data augmentations
  - switched optimizer to AdamW
  -> validation precision scaled rapidly after these changes

Exact hyperparameter values (lr, weight decay, batch size, image size,
loss weights) and exact augmentation settings (which transforms, at what
probability/magnitude) are NOT recoverable for this run -- no hyp.yaml,
args.yaml, or config snapshot was saved alongside the checkpoint (which
itself is also gone). original_report.md only describes changes in
narrative form ("restructured loss weights", "added augmentations"),
never the actual numbers. Confirmed by searching the whole C:\ssd projects
drive for any leftover config/checkpoint from this run -- none found.
Compare to the second run (../training_log.txt, ../full_epoch_log.txt),
where the full hyp.yaml, args.yaml, and all 300 epochs' numbers survive.

Epoch log (mAP50 checkpoints only -- no loss values survive)
--------------------------------------------------------------
epoch  1 : mAP50 = 0.0649
epoch  5 : mAP50 = 0.4284
epoch 10 : mAP50 = 0.5067
epoch 15 : mAP50 = 0.5817  <- best checkpoint (weights not retained)
epoch 20 : mAP50 = 0.5817  <- run end (no improvement epochs 15-20)

Outcome
-------
Completed all 20 epochs without interruption. Plateaued after epoch 15.
Recommendations noted at the time (see original_report.md): move to GPU,
raise input resolution to 640, enable EMA -- all three were applied in
the next run (../ -- "experiment 1 baseline"'s main GPU run), which
reached mAP50 0.907 on the test set.
